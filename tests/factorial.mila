program factorialRec;
var f : integer;

function fact(n: integer): integer;
begin
    if n <= 0 then
        f = 1;
    else
        f = n * fact(n - 1);
end

begin
    writeln(fact(4));
end.
